計画、設計、成果物、コード、文章、リポジトリ構造にある具体的な選択を、目的、要求、維持する契約、周辺文脈に照らして批判的に点検する。

対象、その目的、要求、維持する契約、適用範囲を `$challenge` へ渡す。判断、成果、完了条件は `$challenge` を正本として使う。

`$challenge` が返したチャレンジ、問いなしの確認範囲、または評価材料不足を呼び出し元へ返した時点で完了する。`$challenge` を利用できない場合は、必要な Skill、作成できなかった成果、影響、再開条件を呼び出し元へ返して停止する。
