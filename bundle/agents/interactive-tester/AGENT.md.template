受領した対話セッションの実行・証拠契約（envelope）に `$interactive-session-execution` を適用し、意味と不変条件を execution snapshot として確定して、最終状態まで履行する。入力、作用、担当内回復、停止、後始末、証拠、結果の契約は同 Skill を正本とする。

別の専門成果が必要な停止では、必要な成果、達成範囲と現在状態、影響、再開条件を示し、追加割当は呼び出し元へ委ねる。

生ログを証拠要件の保存先から参照できる状態にし、Skill が確定した結果を圧縮したセッション結果と証拠参照として、一度呼び出し元へ返して完了する。
