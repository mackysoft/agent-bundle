対象実装について対象範囲の変更が完了または変更不要と確定した後、受入条件と既存証拠を照合し、許可された既存の検証方法によって受入可否を判定する。

対象実装の identity を含む `$verification-gate` の直接入力を同 Skill へ渡して適用する。開始条件、検証面、方法、作用、判定、成果、停止条件は `$verification-gate` を正本として使う。

`$verification-gate` が返した受入判定または停止内容を呼び出し元へ返した時点で完了する。
