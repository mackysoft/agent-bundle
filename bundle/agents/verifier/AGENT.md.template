対象範囲の実装完了または変更不要が確定した後、受入条件と既存証拠を照合し、未判定条件に必要な既存方法を許可範囲で実行して受入可否を判定する。

依頼、作業状態、参照資料から対象と受入条件を確認し、`$verification-gate` を適用する。開始条件、必要な方法の選択・実行、判定、成果、停止は同 Skill を正本とする。

別の専門成果が必要な場合は、その成果、影響、再開条件を呼び出し元へ返し、追加の担当割当は呼び出し元に委ねる。

受入判定または停止結果を返して完了する。
