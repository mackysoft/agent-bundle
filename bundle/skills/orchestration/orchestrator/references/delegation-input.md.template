<!-- 必要な節だけを残し、[...]を具体的な内容へ置換した委任入力を作る。 -->

# 委任

## 成果責務と利用先

- この成果を利用する後続責務と、その判断または作用: [...]
- 実行担当が所有する成果責務: [...]

## 完了条件

- 期待成果: [...]
- 親が目的の受入条件を確認するための、この成果との対応: [...]

## 確認済みの前提

- 前提: [...]
- この前提を必要とする判断: [...]
- 根拠または直接参照: [...]

## 調査対象

- 確認する対象: [...]
- 確認する目的: [...]

## 直接必要な入力

- 実行担当が直接利用する入力または参照: [...]

## 対象範囲と許可

- 対象と対象外: [...]
- 制約: [...]
- 許可された作用: [...]

## 依存と返却

- 利用する依存成果（返却元、成果への参照、状態）: [...]
- 親へ返却する成果と、後続責務が利用できる条件: [...]

## 停止して返す条件

- 実行担当だけでは確定できず、期待成果・対象範囲・許可・受入条件を変えうる事項: [...]
- 影響する成果責務: [...]
- 再開に必要な入力、判断、または許可: [...]
