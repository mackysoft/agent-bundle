# 比較範囲台帳

比較範囲の完全性を主張する場合だけ使う。比較基準、最終状態、対象読者、更新対象範囲、最終状態を確認できる走査単位を固定する。

各変更候補を、根拠とともに `記載`、`除外`、`要確認` にする。`記載` は出力エントリへ対応付ける。走査単位または候補に `要確認` が残る間は、その範囲について完全または完了と主張せず、不足事項と再開条件を返す。

一部の範囲だけを完全と主張する場合は、対象範囲と対象外を明示し、対象範囲内の候補をすべて分類する。出力を確定する前に、現在の最終状態と各エントリを再照合する。
